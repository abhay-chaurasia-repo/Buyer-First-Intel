<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Dark base — Light AppCompat leaves white behind status/nav insets on Android 15/16. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowBackground">@color/bfi_night</item>
        <item name="android:colorBackground">@color/bfi_night</item>
        <item name="android:statusBarColor">@color/bfi_night</item>
        <item name="android:navigationBarColor">@color/bfi_night</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:windowLightNavigationBar">false</item>
    </style>

    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:background">@null</item>
        <item name="android:windowBackground">@color/bfi_night</item>
        <item name="android:colorBackground">@color/bfi_night</item>
        <item name="android:statusBarColor">@color/bfi_night</item>
        <item name="android:navigationBarColor">@color/bfi_night</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:windowLightNavigationBar">false</item>
    </style>

    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/bfi_night</item>
        <item name="postSplashScreenTheme">@style/AppTheme.NoActionBar</item>
        <item name="android:windowBackground">@color/bfi_night</item>
        <item name="android:statusBarColor">@color/bfi_night</item>
        <item name="android:navigationBarColor">@color/bfi_night</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:windowLightNavigationBar">false</item>
    </style>
</resources>
